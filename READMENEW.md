`npm start`
runs on http://localhost:5173/

Linting:

`npx prettier . --check`
`npx prettier . --write`

- vite project

• format code

• write a readme

• delete old branch and main

• push new code

if rootinf doesn't exist send to a 404 page
